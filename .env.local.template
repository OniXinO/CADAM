VITE_SUPABASE_ANON_KEY="<Test Anon Key>"
VITE_SUPABASE_URL='http://127.0.0.1:54321'
VITE_POSTHOG_PROJECT_KEY="<Test PostHog Project Key>"
VITE_SENTRY_ENVIRONMENT="local"
VITE_SENTRY_DSN="<Sentry DSN>"
# Preview-only escape hatch: when "true", AuthGuard auto-creates an
# anonymous Supabase session instead of redirecting to /signin. Pair with
# PREVIEW_BYPASS_BILLING=true on the edge-function env so anonymous users
# (no email) can exercise parametric-chat. Leave unset for production.
VITE_PREVIEW_DEMO_MODE=""
